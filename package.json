{
  "name": "esplant-workspace",
  "private": true,
  "description": "Root manifest so Netlify installs the Functions dependency (@netlify/blobs). Frontend deps live in frontend/package.json.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
