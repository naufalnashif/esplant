# Esplan — Netlify deployment configuration
# Static Vite build outputted to frontend/dist

[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"

# SPA Fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
