# Crash report
Page: /
## Error
runtime: useStorage must be used within a StorageProvider
at src/lib/storageContext.tsx:128:23
```
  126 | export const useStorage = (): StorageContextValue => {
  127 |   const context = useContext(StorageContext);
> 128 |   if (!context) throw new Error("useStorage must be used within a StorageProvider");
      |                       ^
  129 |   return context;
  130 | };
```
## Session replay
(recorder off — no interaction events captured; set EMERGENT_OVERLAY_RRWEB=buffer on the pod to get pre-crash timelines)