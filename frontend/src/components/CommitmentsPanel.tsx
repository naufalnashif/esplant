import { CalendarClock, Pencil, Plus, Trash2 } from "lucide-react";
import { toast } from "sonner";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import type { Bill, CommitmentType, Debt, FinanceState } from "@/lib/localDb";
import { useState } from "react";
import type * as React from "react";

export function CommitmentsPanel({ state, onSave }: { state: FinanceState; onSave: (next: FinanceState) => void }) {
  const [bill, setBill] = useState({ name: "", category: "Utilities", amount: "", frequency: "monthly" as Bill["frequency"], nextDueDate: new Date().toISOString().slice(0, 10), remainingInstallments: "" });
  const [debt, setDebt] = useState({ name: "", person: "", type: "debt" as CommitmentType, total: "", dueDate: new Date().toISOString().slice(0, 10) });
  const addBill = (event: React.FormEvent) => { event.preventDefault(); const amount = Number(bill.amount); if (!bill.name.trim() || !Number.isFinite(amount) || amount <= 0) return; const next: Bill = { id: `bill-${Date.now()}`, name: bill.name.trim(), category: bill.category, amount, currency: state.baseCurrency, frequency: bill.frequency, nextDueDate: bill.nextDueDate, remainingInstallments: bill.remainingInstallments ? Number(bill.remainingInstallments) : undefined, active: true }; onSave({ ...state, bills: [next, ...state.bills] }); setBill({ name: "", category: "Utilities", amount: "", frequency: "monthly", nextDueDate: new Date().toISOString().slice(0, 10), remainingInstallments: "" }); toast.success("Bill created."); };
  const addDebt = (event: React.FormEvent) => { event.preventDefault(); const total = Number(debt.total); if (!debt.name.trim() || !debt.person.trim() || !Number.isFinite(total) || total <= 0) return; const next: Debt = { id: `debt-${Date.now()}`, name: debt.name.trim(), person: debt.person.trim(), type: debt.type, total, paid: 0, currency: state.baseCurrency, dueDate: debt.dueDate, note: "" }; onSave({ ...state, debts: [next, ...state.debts] }); setDebt({ name: "", person: "", type: "debt", total: "", dueDate: new Date().toISOString().slice(0, 10) }); toast.success("Debt or receivable created."); };
  const editBill = (item: Bill) => { const value = Number(window.prompt(`New amount for ${item.name}`, String(item.amount))); if (!Number.isFinite(value) || value <= 0) return; onSave({ ...state, bills: state.bills.map((billItem) => billItem.id === item.id ? { ...billItem, amount: value } : billItem) }); toast.success("Bill updated."); };
  const editDebt = (item: Debt) => { const value = Number(window.prompt(`New total for ${item.name}`, String(item.total))); if (!Number.isFinite(value) || value <= 0) return; onSave({ ...state, debts: state.debts.map((debtItem) => debtItem.id === item.id ? { ...debtItem, total: value, paid: Math.min(debtItem.paid, value) } : debtItem) }); toast.success("Debt updated."); };
  const remove = (kind: "bill" | "debt", id: string) => { if (!window.confirm(`Delete this ${kind}?`)) return; onSave({ ...state, bills: kind === "bill" ? state.bills.filter((item) => item.id !== id) : state.bills, debts: kind === "debt" ? state.debts.filter((item) => item.id !== id) : state.debts }); toast.success(`${kind} deleted.`); };
  return <div className="animate-rise-in"><div className="mb-6"><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.22em] text-primary">Obligations / commitments</p><h1 className="font-heading text-3xl font-extrabold tracking-tight sm:text-4xl">Bills & debt</h1><p className="mt-2 max-w-2xl text-sm leading-relaxed text-muted-foreground">Kelola tagihan berulang, cicilan, utang, dan piutang dengan operasi create, read, update, delete.</p></div><div className="grid gap-6 xl:grid-cols-2"><section className="rounded-2xl border border-border/70 bg-card/75 p-5 shadow-sm sm:p-6"><div className="mb-4 flex items-center justify-between"><h2 className="font-heading text-xl font-bold">Recurring bills</h2><Badge variant="secondary">{state.bills.length}</Badge></div><div className="space-y-2">{state.bills.map((item) => <div key={item.id} className="flex items-center gap-3 rounded-xl border border-border/60 p-3"><div className="grid size-9 place-items-center rounded-lg bg-amber-500/12 text-amber-400"><CalendarClock size={16} /></div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{item.name}</p><p className="text-[11px] text-muted-foreground">{item.category} · {item.nextDueDate} · {item.remainingInstallments ?? "∞"}x</p></div><span className="font-data text-xs">{item.amount.toLocaleString("id-ID")}</span><button type="button" data-testid={`bill-edit-crud-${item.id}-button`} onClick={() => editBill(item)} className="grid size-7 place-items-center rounded text-muted-foreground hover:text-primary"><Pencil size={13} /></button><button type="button" data-testid={`bill-delete-crud-${item.id}-button`} onClick={() => remove("bill", item.id)} className="grid size-7 place-items-center rounded text-muted-foreground hover:text-red-400"><Trash2 size={13} /></button></div>)}</div><form onSubmit={addBill} className="mt-4 grid gap-2 sm:grid-cols-2"><input data-testid="bill-name-input" required value={bill.name} onChange={(event) => setBill((value) => ({ ...value, name: event.target.value }))} placeholder="Bill name" className="h-10 rounded-lg border border-border bg-background px-3 text-sm" /><input data-testid="bill-amount-input" required type="number" min="1" value={bill.amount} onChange={(event) => setBill((value) => ({ ...value, amount: event.target.value }))} placeholder="Amount" className="h-10 rounded-lg border border-border bg-background px-3 font-data text-sm" /><input data-testid="bill-date-input" required type="date" value={bill.nextDueDate} onChange={(event) => setBill((value) => ({ ...value, nextDueDate: event.target.value }))} className="h-10 rounded-lg border border-border bg-background px-3 text-xs" /><Button data-testid="bill-create-button" type="submit" className="h-10 gap-2"><Plus size={14} />Create bill</Button></form></section><section className="rounded-2xl border border-border/70 bg-card/75 p-5 shadow-sm sm:p-6"><div className="mb-4 flex items-center justify-between"><h2 className="font-heading text-xl font-bold">Debt & receivables</h2><Badge variant="secondary">{state.debts.length}</Badge></div><div className="space-y-2">{state.debts.map((item) => <div key={item.id} className="flex items-center gap-3 rounded-xl border border-border/60 p-3"><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{item.name}</p><p className="text-[11px] text-muted-foreground">{item.person} · {item.type} · {item.paid.toLocaleString("id-ID")} paid</p></div><button type="button" data-testid={`debt-edit-crud-${item.id}-button`} onClick={() => editDebt(item)} className="grid size-7 place-items-center rounded text-muted-foreground hover:text-primary"><Pencil size={13} /></button><button type="button" data-testid={`debt-delete-crud-${item.id}-button`} onClick={() => remove("debt", item.id)} className="grid size-7 place-items-center rounded text-muted-foreground hover:text-red-400"><Trash2 size={13} /></button></div>)}</div><form onSubmit={addDebt} className="mt-4 grid gap-2 sm:grid-cols-2"><input data-testid="commitment-name-input" required value={debt.name} onChange={(event) => setDebt((value) => ({ ...value, name: event.target.value }))} placeholder="Name" className="h-10 rounded-lg border border-border bg-background px-3 text-sm" /><input data-testid="commitment-person-input" required value={debt.person} onChange={(event) => setDebt((value) => ({ ...value, person: event.target.value }))} placeholder="Person" className="h-10 rounded-lg border border-border bg-background px-3 text-sm" /><input data-testid="commitment-total-input" required type="number" min="1" value={debt.total} onChange={(event) => setDebt((value) => ({ ...value, total: event.target.value }))} placeholder="Total" className="h-10 rounded-lg border border-border bg-background px-3 font-data text-sm" /><Button data-testid="commitment-create-button" type="submit" className="h-10 gap-2"><Plus size={14} />Create</Button></form></section></div></div>;
}