import { CalendarClock, Download, FileText } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import type { FinanceState } from "@/lib/localDb";

const money = (value: number, locale: FinanceState["locale"]) => new Intl.NumberFormat(locale === "id" ? "id-ID" : "en-US", { style: "currency", currency: "IDR", maximumFractionDigits: 0 }).format(value);
export function ReportCenter({ state, currentMonth, onPrint }: { state: FinanceState; currentMonth: string; onPrint: () => void }) {
  const items = state.transactions.filter((item) => item.date.slice(0, 7) === currentMonth);
  const expense = items.filter((item) => item.kind === "expense").reduce((sum, item) => sum + item.baseAmount, 0);
  const income = items.filter((item) => item.kind === "income").reduce((sum, item) => sum + item.baseAmount, 0);
  const nextRun = new Date(); nextRun.setDate(nextRun.getDate() + 1);
  return <section className="mt-6 rounded-2xl border border-border/70 bg-card/75 p-5 shadow-sm backdrop-blur-xl sm:p-6" data-testid="report-center"><div className="mb-5 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div className="flex items-start gap-3"><div className="grid size-10 place-items-center rounded-xl bg-indigo-500/12 text-indigo-400"><FileText size={18} /></div><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-indigo-400">Report center</p><h2 className="font-heading text-xl font-bold">Preview & download PDF</h2><p className="mt-1 text-xs text-muted-foreground">Preview mengikuti angka yang sama dengan dashboard dan dicetak langsung dari browser.</p></div></div><div className="flex gap-2"><Badge variant="secondary" className="gap-1"><CalendarClock size={12} /> Daily default</Badge><Button data-testid="report-pdf-button" onClick={onPrint} className="gap-2"><Download size={15} /> Download PDF</Button></div></div><div data-testid="report-print-surface" className="rounded-xl border border-border/60 bg-background/45 p-5"><div className="flex items-start justify-between gap-4 border-b border-border/60 pb-4"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-primary">NusaArtha · Personal report</p><h3 className="mt-1 font-heading text-xl font-bold">Monthly spending report</h3><p className="mt-1 text-xs text-muted-foreground">Period {currentMonth} · {state.profileName}</p></div><p className="font-data text-xs text-muted-foreground">{new Date().toLocaleDateString(state.locale === "id" ? "id-ID" : "en-US")}</p></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div><p className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Income</p><p className="mt-1 font-data text-lg font-bold text-emerald-400">{money(income, state.locale)}</p></div><div><p className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Expense</p><p className="mt-1 font-data text-lg font-bold text-rose-400">{money(expense, state.locale)}</p></div><div><p className="text-[10px] font-semibold uppercase tracking-wider text-muted-foreground">Net flow</p><p className="mt-1 font-data text-lg font-bold">{money(income - expense, state.locale)}</p></div></div><div className="mt-5 overflow-hidden rounded-lg border border-border/60"><table className="w-full text-left text-xs"><thead className="bg-secondary/35 text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="px-3 py-2">Date</th><th className="px-3 py-2">Merchant</th><th className="px-3 py-2 text-right">Amount</th></tr></thead><tbody className="divide-y divide-border/50">{items.slice(0, 6).map((item) => <tr key={item.id}><td className="px-3 py-2 text-muted-foreground">{item.date}</td><td className="px-3 py-2 font-semibold">{item.description}</td><td className="px-3 py-2 text-right font-data">{item.kind === "income" ? "+" : "−"}{money(item.baseAmount, state.locale)}</td></tr>)}</tbody></table></div></div><p className="mt-3 flex items-center gap-2 text-[10px] text-muted-foreground"><CalendarClock size={13} className="text-primary" /> Next browser reminder: {nextRun.toLocaleDateString(state.locale === "id" ? "id-ID" : "en-US")} · email Resend remains <strong className="text-amber-400">MOCKED / NOT ACTIVE</strong></p></section>;
}