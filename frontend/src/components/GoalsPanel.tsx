import { ArrowDownToLine, ArrowUpFromLine, Check, Lightbulb, Plus, Target } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import type { Currency, FinanceState, SavingsGoal, WishlistItem } from "@/lib/localDb";
import { useState } from "react";
import type * as React from "react";

const wishlistCategories = ["Travel", "Work", "Home", "Personal", "Education", "Health", "Other"];
const money = (value: number, currency: Currency, locale: FinanceState["locale"]) => new Intl.NumberFormat(locale === "id" ? "id-ID" : "en-US", { style: "currency", currency, maximumFractionDigits: currency === "IDR" ? 0 : 2, notation: value > 1000000 ? "compact" : "standard" }).format(value);

export function GoalsPanel({ state, wishForm, setWishForm, onAddWish, onCommit }: { state: FinanceState; wishForm: { name: string; price: string; priority: WishlistItem["priority"]; targetDate: string; category: string }; setWishForm: React.Dispatch<React.SetStateAction<{ name: string; price: string; priority: WishlistItem["priority"] ; targetDate: string; category: string }>>; onAddWish: (event: React.FormEvent) => void; onCommit: (goal: SavingsGoal, amount: number, direction: "deposit" | "withdraw") => void }) {
  const [category, setCategory] = useState("all");
  const [goalId, setGoalId] = useState(state.savings[0]?.id ?? "");
  const [amount, setAmount] = useState("");
  const [direction, setDirection] = useState<"deposit" | "withdraw">("deposit");
  const displayed = category === "all" ? state.wishlist : state.wishlist.filter((item) => item.category === category);
  const savedTotal = state.savings.reduce((sum, goal) => sum + goal.saved * state.exchangeRates[goal.currency], 0) / (state.exchangeRates[state.baseCurrency] || 1);
  const submitCommit = (event: React.FormEvent) => { event.preventDefault(); const goal = state.savings.find((item) => item.id === goalId); const value = Number(amount); if (!goal || !Number.isFinite(value) || value <= 0) return; onCommit(goal, value, direction); setAmount(""); };
  return <div className="animate-rise-in"><div className="mb-6"><p className="mb-2 text-[10px] font-bold uppercase tracking-[0.22em] text-primary">Future you / goals</p><h1 className="font-heading text-3xl font-extrabold tracking-tight sm:text-4xl">Goals & wishlist</h1><p className="mt-2 max-w-2xl text-sm leading-relaxed text-muted-foreground">Tabungan adalah bucket uang yang nyata; wishlist adalah rencana yang terpisah.</p></div><div className="mb-6 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-primary/20 bg-primary/8 p-5"><p className="text-xs font-semibold text-muted-foreground">Total uang tertabung</p><p className="mt-2 font-heading text-2xl font-extrabold" data-testid="total-savings-value">{money(savedTotal, state.baseCurrency, state.locale)}</p><p className="mt-1 text-[10px] text-primary">Across {state.savings.length} goals</p></div><div className="rounded-2xl border border-border/70 bg-card/75 p-5"><p className="text-xs font-semibold text-muted-foreground">Target savings</p><p className="mt-2 font-heading text-2xl font-extrabold">{money(state.savings.reduce((sum, goal) => sum + goal.target, 0), state.baseCurrency, state.locale)}</p><p className="mt-1 text-[10px] text-muted-foreground">Commitment progress</p></div><div className="rounded-2xl border border-border/70 bg-card/75 p-5"><p className="text-xs font-semibold text-muted-foreground">Wishlist items</p><p className="mt-2 font-heading text-2xl font-extrabold">{state.wishlist.length}</p><p className="mt-1 text-[10px] text-muted-foreground">Filtered by category below</p></div></div><div className="grid gap-6 xl:grid-cols-[1.1fr_1fr]"><div className="space-y-6"><section className="rounded-2xl border border-border/70 bg-card/75 p-5 shadow-sm backdrop-blur-xl sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground">Savings buckets</p><h2 className="mt-1 font-heading text-xl font-bold">Celengan & tabungan</h2></div><Target size={19} className="text-primary" /></div><div className="grid gap-4 sm:grid-cols-2">{state.savings.map((goal) => <div key={goal.id} className="rounded-2xl border border-border/60 bg-background/35 p-4"><div className="mb-4 flex items-start justify-between"><div className="grid size-10 place-items-center rounded-xl bg-primary/12 text-primary"><Target size={18} /></div><Badge variant="outline">{Math.round((goal.saved / goal.target) * 100)}%</Badge></div><p className="font-heading text-base font-bold">{goal.name}</p><div className="mt-4 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-primary" style={{ width: `${Math.min(100, goal.saved / goal.target * 100)}%` }} /></div><div className="mt-2 flex justify-between text-[10px] font-semibold text-muted-foreground"><span>{money(goal.saved, goal.currency, state.locale)}</span><span>{money(goal.target, goal.currency, state.locale)}</span></div></div>)}</div></section><section className="rounded-2xl border border-primary/20 bg-primary/5 p-5 sm:p-6"><div className="mb-4 flex items-center gap-2"><ArrowUpFromLine size={17} className="text-primary" /><h2 className="font-heading text-lg font-bold">Commit to savings</h2></div><form onSubmit={submitCommit} className="grid gap-3 sm:grid-cols-[1fr_1fr_auto]"><select data-testid="savings-goal-select" value={goalId} onChange={(event) => setGoalId(event.target.value)} className="h-10 rounded-lg border border-border bg-background px-3 text-xs font-semibold">{state.savings.map((goal) => <option key={goal.id} value={goal.id} label={goal.name} />)}</select><input data-testid="savings-commit-amount-input" required type="number" min="1" value={amount} onChange={(event) => setAmount(event.target.value)} placeholder="Nominal" className="h-10 rounded-lg border border-border bg-background px-3 font-data text-xs outline-none focus:border-primary" /><Button data-testid="savings-commit-submit-button" type="submit" className="h-10 gap-2"><Check size={14} />{direction === "deposit" ? "Commit" : "Withdraw"}</Button></form><div className="mt-3 flex gap-2"><button type="button" data-testid="savings-deposit-toggle" onClick={() => setDirection("deposit")} className={`flex items-center gap-1 rounded-lg px-3 py-2 text-[10px] font-bold ${direction === "deposit" ? "bg-primary text-primary-foreground" : "border border-border text-muted-foreground"}`}><ArrowUpFromLine size={12} /> Menabung</button><button type="button" data-testid="savings-withdraw-toggle" onClick={() => setDirection("withdraw")} className={`flex items-center gap-1 rounded-lg px-3 py-2 text-[10px] font-bold ${direction === "withdraw" ? "bg-amber-400 text-slate-900" : "border border-border text-muted-foreground"}`}><ArrowDownToLine size={12} /> Ambil tabungan</button></div><p className="mt-3 text-[10px] leading-relaxed text-muted-foreground">Commit mengurangi saldo akun dan membuat transaksi Savings. Withdraw melakukan kebalikannya.</p></section></div><div className="space-y-6"><section className="rounded-2xl border border-border/70 bg-card/75 p-5 shadow-sm backdrop-blur-xl sm:p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground">Wants, with intent</p><h2 className="mt-1 font-heading text-xl font-bold">Wishlist by category</h2></div><select data-testid="wishlist-category-filter" value={category} onChange={(event) => setCategory(event.target.value)} className="h-9 rounded-lg border border-border bg-background px-3 text-xs font-semibold"><option value="all" label="All categories" />{wishlistCategories.map((item) => <option key={item} value={item} label={item} />)}</select></div><div className="space-y-3">{displayed.map((wish) => <div key={wish.id} className="flex items-center gap-3 rounded-xl border border-border/60 bg-background/35 p-3"><div className="grid size-9 place-items-center rounded-lg bg-amber-500/12 text-amber-400"><Lightbulb size={16} /></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><p className="truncate text-sm font-semibold">{wish.name}</p><Badge variant="outline" className="text-[9px]">{wish.category}</Badge></div><p className="mt-1 text-[11px] text-muted-foreground">{wish.status} · {money(wish.price, wish.currency, state.locale)}</p></div></div>)}{!displayed.length && <p className="py-8 text-center text-sm text-muted-foreground">No wishlist items in this category.</p>}</div></section><section className="rounded-2xl border border-border/70 bg-card/75 p-5 shadow-sm backdrop-blur-xl sm:p-6"><div className="mb-4 flex items-center gap-2"><Plus size={17} className="text-primary" /><h2 className="font-heading text-lg font-bold">Add wishlist</h2></div><form onSubmit={onAddWish} className="grid gap-3 sm:grid-cols-2"><input data-testid="wishlist-name-input" required value={wishForm.name} onChange={(event) => setWishForm((form) => ({ ...form, name: event.target.value }))} placeholder="Apa yang kamu inginkan?" className="h-10 rounded-lg border border-border bg-background px-3 text-sm outline-none focus:border-primary sm:col-span-2" /><input data-testid="wishlist-price-input" required type="number" min="1" value={wishForm.price} onChange={(event) => setWishForm((form) => ({ ...form, price: event.target.value }))} placeholder="Harga target" className="h-10 rounded-lg border border-border bg-background px-3 font-data text-sm outline-none focus:border-primary" /><select data-testid="wishlist-category-select" value={wishForm.category} onChange={(event) => setWishForm((form) => ({ ...form, category: event.target.value }))} className="h-10 rounded-lg border border-border bg-background px-3 text-xs font-semibold">{wishlistCategories.map((item) => <option key={item} value={item} label={item} />)}</select><select data-testid="wishlist-priority-select" value={wishForm.priority} onChange={(event) => setWishForm((form) => ({ ...form, priority: event.target.value as WishlistItem["priority"] }))} className="h-10 rounded-lg border border-border bg-background px-3 text-xs font-semibold"><option value="high" label="High priority" /><option value="medium" label="Medium priority" /><option value="low" label="Low priority" /></select><Button data-testid="wishlist-submit-button" type="submit" className="h-10">Save wishlist</Button></form></section></div></div></div>;
}